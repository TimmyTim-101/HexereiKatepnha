<Window x:Class="HexereiKatepnha.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HexereiKatepnha"
        xmlns:ViewModels="clr-namespace:HexereiKatepnha.ViewModels"
        xmlns:Views="clr-namespace:HexereiKatepnha.Views"
        mc:Ignorable="d"
        Title="魔导Katep&#x438;Ha" Height="1080" Width="1920"
        WindowStartupLocation="CenterScreen"
        Icon="Resources/Icon/Icon-maskable-512.png"
        FontFamily="{StaticResource MyFont}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type ViewModels:HomeViewModel}">
            <Views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type ViewModels:SettingsViewModel}">
            <Views:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type ViewModels:DatabaseViewModel}">
            <Views:DatabaseView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
            </Grid.RowDefinitions>
            
            <Views:NavigationView Grid.Row="0" />
            <TextBlock Grid.Row="1" Text="Settings~"></TextBlock>
        </Grid>
        <!-- 内容页 -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" Margin="0" />
    </Grid>
</Window>