<Window x:Class="HexereiKatepnha.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HexereiKatepnha"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Margin="20" VerticalAlignment="Center">

        <TextBlock Text="请输入你的名字:" FontSize="14" Margin="0,0,0,5"/>

        <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" 
                 Height="30" FontSize="14" Padding="5"/>

        <Button Content="点击生成问候" 
                Command="{Binding SayHelloCommand}"
                Margin="0,20,0,0" Height="35" Background="#007ACC" Foreground="White"/>

        <TextBlock Text="{Binding GreetingMessage}" 
                   FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,20,0,0"
                   Foreground="#333"/>
    </StackPanel>
</Window>
