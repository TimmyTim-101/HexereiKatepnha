<UserControl x:Class="HexereiKatepnha.Views.Database.Database3ArtifactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1720"
             Background="#FF1E1E1E"
             FontFamily="{StaticResource MyFont}">
    <Grid Background="{DynamicResource GlobalBackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="圣遗物" Style="{StaticResource MyTextBlock}" FontSize="30" Margin="30,20,30,5" />
        <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewer}">
            <StackPanel>
                <Expander Margin="20">
                    <Expander.Header>
                        <TextBlock Text="圣遗物相关详细信息" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                    </Expander.Header>
                    <StackPanel Orientation="Vertical" Margin="20,5">
                        <TextBlock Text="具体信息" Style="{StaticResource TodoTextBlock}"></TextBlock>
                        <Border Style="{StaticResource HorizontalDivider}"></Border>
                    </StackPanel>
                </Expander>
                <ItemsControl ItemsSource="{Binding AllDatabase3ArtifactModelsList}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="549"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5,5,0,0" CornerRadius="5" BorderBrush="White" BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"></RowDefinition>
                                            <RowDefinition Height="*"></RowDefinition>
                                            <RowDefinition Height="*"></RowDefinition>
                                            <RowDefinition Height="*"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
                                            <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding Name}"></TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                                            <TextBlock Style="{StaticResource MyTextBlock}" FontSize="10" Foreground="#777777">
                                                <Run Text="ID:"></Run>
                                                <Run Text="{Binding Vid}"></Run>
                                            </TextBlock>
                                        </StackPanel>
                                        <Border Grid.Row="2" Style="{StaticResource HorizontalDivider}"></Border>
                                        <Grid Grid.Row="3">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                <ColumnDefinition Width="*"></ColumnDefinition>
                                            </Grid.ColumnDefinitions>
                                            <ItemsControl Grid.Column="0" ItemsSource="{Binding ImageList}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center"></StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                            <Image Source="{Binding ArtifactIconPath, IsAsync=True}" Width="50" Height="50"></Image>
                                                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,15,0">
                                                                <TextBlock Text="{Binding Name}" Style="{StaticResource MyTextBlock}" FontSize="16"></TextBlock>
                                                            </StackPanel>
                                                            <ItemsControl ItemsSource="{Binding ImagePathList}">
                                                                <ItemsControl.ItemsPanel>
                                                                    <ItemsPanelTemplate>
                                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left"></StackPanel>
                                                                    </ItemsPanelTemplate>
                                                                </ItemsControl.ItemsPanel>
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <Grid Width="50" Height="50" ToolTipService.InitialShowDelay="100">
                                                                            <Grid.ToolTip>
                                                                                <ToolTip Background="Transparent" BorderThickness="0">
                                                                                    <Grid Width="250" Height="250">
                                                                                        <Image Source="{Binding BackgroundPath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                                                        <Image Source="{Binding ImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                                                    </Grid>
                                                                                </ToolTip>
                                                                            </Grid.ToolTip>
                                                                            <Image Source="{Binding BackgroundPath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                                            <Image Source="{Binding ImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                                        </Grid>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <Border Grid.Column="1" Style="{StaticResource VerticalDivider}"></Border>
                                            <ItemsControl Grid.Column="2" ItemsSource="{Binding EffectList}" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock FontSize="16" Style="{StaticResource MyTextBlock}" TextWrapping="Wrap" Margin="5">
                                                            <Run Text="{Binding Num, StringFormat='{}{0}件套: '}" Foreground="#EBB30B"></Run>
                                                            <Run Text="{Binding Effect}"></Run>
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Grid>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>