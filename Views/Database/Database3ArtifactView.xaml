<UserControl x:Class="HexereiKatepnha.Views.Database.Database3ArtifactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:models="clr-namespace:HexereiKatepnha.Models.ModelsForViews.Database"
             xmlns:vm="clr-namespace:HexereiKatepnha.ViewModels.Database"
             d:DataContext="{d:DesignInstance vm:Database3ArtifactViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1720"
             Background="{DynamicResource GlobalBackColor}"
             FontFamily="{StaticResource MyFont}">
    <UserControl.Resources>
        <DataTemplate x:Key="ArtifactEffectTemplate" DataType="{x:Type models:Database3ArtifactEffectModel}">
            <TextBlock FontSize="16" Style="{StaticResource MyTextBlock}" TextWrapping="Wrap" Margin="5">
                <Run Text="{Binding Num, StringFormat='{}{0}件套: '}" Foreground="#EBB30B"></Run>
                <Run Text="{Binding Effect}"></Run>
            </TextBlock>
        </DataTemplate>
        <DataTemplate x:Key="ArtifactPositionImageTemplate" DataType="{x:Type models:Database3ArtifactRarityModel}">
            <Border BorderThickness="0" Background="Transparent" Height="50" Width="50">
                <Grid ToolTipService.InitialShowDelay="100">
                    <Grid.ToolTip>
                        <ToolTip Background="Transparent" BorderThickness="0">
                            <Grid Width="250" Height="250">
                                <Image Source="{Binding BackgroundImagePath}" Stretch="UniformToFill"></Image>
                                <Image Source="{Binding ImagePath}" Stretch="UniformToFill"></Image>
                            </Grid>
                        </ToolTip>
                    </Grid.ToolTip>
                    <Image Source="{Binding BackgroundImagePath}" Height="50" Width="50" Stretch="UniformToFill"></Image>
                    <Image Source="{Binding ImagePath}" Height="50" Width="50" Stretch="UniformToFill"></Image>
                </Grid>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="ArtifactPositionTemplate" DataType="{x:Type models:Database3ArtifactPositionModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"></ColumnDefinition>
                    <ColumnDefinition Width="120"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Source="{Binding PositionIconPath}" Height="50" Width="50" Stretch="UniformToFill"></Image>
                <Viewbox Grid.Column="1" MaxWidth="115" StretchDirection="DownOnly" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding PositionName}" FontSize="16"></TextBlock>
                </Viewbox>
                <ItemsControl Grid.Column="2" ItemsSource="{Binding PositionRarityList}" ItemTemplate="{StaticResource ArtifactPositionImageTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="ArtifactCardTemplate" DataType="{x:Type models:Database3ArtifactModel}">
            <Border CornerRadius="5" BorderBrush="White" BorderThickness="1" Height="301" Width="549" Margin="2" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Style="{StaticResource MyTextBlock}" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                    <TextBlock Grid.Row="1" Style="{StaticResource MyTextBlock}" FontSize="10" Foreground="#777777" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Run Text="ID:"></Run>
                        <Run Text="{Binding Vid}"></Run>
                    </TextBlock>
                    <Border Grid.Row="2" Style="{StaticResource HorizontalDivider}"></Border>
                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <ItemsControl Grid.Column="0" ItemsSource="{Binding PositionList}" ItemTemplate="{StaticResource ArtifactPositionTemplate}" HorizontalAlignment="Center" VerticalAlignment="Center"></ItemsControl>
                        <Border Grid.Column="1" Style="{StaticResource VerticalDivider}"></Border>
                        <ui:ItemsRepeaterScrollHost Grid.Column="2">
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewer}" HorizontalAlignment="Center" VerticalAlignment="Center" PreviewMouseWheel="InnerScrollViewerPreviewMouseWheel">
                                <ui:ItemsRepeater ItemsSource="{Binding EffectList}" ItemTemplate="{StaticResource ArtifactEffectTemplate}">
                                    <ui:ItemsRepeater.Layout>
                                        <ui:StackLayout Orientation="Vertical"></ui:StackLayout>
                                    </ui:ItemsRepeater.Layout>
                                </ui:ItemsRepeater>
                            </ScrollViewer>
                        </ui:ItemsRepeaterScrollHost>
                    </Grid>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Background="{DynamicResource GlobalBackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="30,20,30,5">
            <TextBlock Text="圣遗物" Style="{StaticResource MyTextBlock}" FontSize="30"></TextBlock>
            <Button Style="{StaticResource ImageButton}" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTipService.InitialShowDelay="100" Margin="10,0,0,0" Command="{Binding OpenHelpWebPageCommand}">
                <Image Source="/Resources/Icons/help_30dp_DDDDDD_FILL0_wght400_GRAD0_opsz24.png" Height="30" Width="30" Stretch="UniformToFill"></Image>
                <Button.ToolTip>
                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="帮助"></ToolTip>
                </Button.ToolTip>
            </Button>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
        <Expander Grid.Row="2" Margin="20" ui:ThemeManager.RequestedTheme="Dark" MaxHeight="400">
            <Expander.Header>
                <TextBlock Text="圣遗物相关详细信息" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
            </Expander.Header>
            <StackPanel Orientation="Vertical" Margin="20,5">
                <TextBlock Text="具体信息" Style="{StaticResource TodoTextBlock}"></TextBlock>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
            </StackPanel>
        </Expander>
        <ui:ItemsRepeaterScrollHost Grid.Row="3">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewer}">
                <ui:ItemsRepeater ItemsSource="{Binding AllDatabase3ArtifactModelsList}" ItemTemplate="{StaticResource ArtifactCardTemplate}">
                    <ui:ItemsRepeater.Layout>
                        <ui:UniformGridLayout Orientation="Horizontal"></ui:UniformGridLayout>
                    </ui:ItemsRepeater.Layout>
                </ui:ItemsRepeater>
            </ScrollViewer>
        </ui:ItemsRepeaterScrollHost>
    </Grid>
</UserControl>