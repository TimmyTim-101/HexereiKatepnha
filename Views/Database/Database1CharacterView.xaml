<UserControl x:Class="HexereiKatepnha.Views.Database.Database1CharacterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#FF1E1E1E"
             d:DesignHeight="1080" d:DesignWidth="1720"
             FontFamily="{StaticResource MyFont}">
    <Grid Background="{DynamicResource GlobalBackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="角色" Style="{StaticResource MyTextBlock}" FontSize="30" Margin="30,20,30,5" />
        <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
        <Grid Grid.Row="2" Margin="30,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <CheckBox Grid.Column="0" Content="显示详细数据" IsChecked="{Binding IsShowMoreNumbers}" HorizontalAlignment="Center" Style="{StaticResource ModernCheckBoxStyle}" VerticalAlignment="Center"></CheckBox>
        </Grid>
        <Border Grid.Row="3" Style="{StaticResource HorizontalDivider}"></Border>
        <ItemsControl Grid.Row="4" ItemsSource="{Binding AllCharacterList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="320"></WrapPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="5,5,0,0" CornerRadius="5" BorderBrush="White" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Vertical">
                                    <Grid ToolTipService.InitialShowDelay="100">
                                        <Grid.ToolTip>
                                            <ToolTip Background="Transparent" BorderThickness="0">
                                                <Grid Width="250" Height="250">
                                                    <Image Source="{Binding BackgroundImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                    <Image Source="{Binding ImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                </Grid>
                                            </ToolTip>
                                        </Grid.ToolTip>
                                        <Image Source="{Binding BackgroundImagePath, IsAsync=True}" Width="150" Height="150" Stretch="UniformToFill"></Image>
                                        <Image Source="{Binding ImagePath, IsAsync=True}" Width="150" Height="150" Stretch="UniformToFill"></Image>
                                        <Image Source="{Binding ElementImagePath, IsAsync=True}" Width="40" Height="40" Stretch="UniformToFill" HorizontalAlignment="Left" VerticalAlignment="Top"></Image>
                                    </Grid>
                                    <Image Source="{Binding StarImagePath, IsAsync=True}" Height="20" HorizontalAlignment="Center"></Image>
                                    <Viewbox MaxWidth="150" StretchDirection="DownOnly" Margin="0,0,0,0">
                                        <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" TextTrimming="CharacterEllipsis"></TextBlock>
                                    </Viewbox>
                                    <TextBlock Style="{StaticResource MyTextBlock}" FontSize="10" Foreground="#777777" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0">
                                        <Run Text="ID:"></Run>
                                        <Run Text="{Binding Vid}"></Run>
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding BirthdayString}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0"></TextBlock>
                                </StackPanel>
                                <Border Grid.Column="1" Style="{StaticResource VerticalDivider}"></Border>
                                <Grid Grid.Column="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"></RowDefinition>
                                        <RowDefinition Height="Auto"></RowDefinition>
                                        <RowDefinition Height="*"></RowDefinition>
                                        <RowDefinition Height="Auto"></RowDefinition>
                                        <RowDefinition Height="Auto"></RowDefinition>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Image Source="{Binding WeaponTypeImagePath}" Height="25" Width="25" Stretch="UniformToFill" Margin="10,0,10,0"></Image>
                                        <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding WeaponTypeName}" Margin="0,0,0,0"></TextBlock>
                                    </StackPanel>
                                    <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
                                    <ItemsControl Grid.Row="2" ItemsSource="{Binding NeedMaterialList}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"></WrapPanel>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="2">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"></RowDefinition>
                                                    </Grid.RowDefinitions>
                                                    <StackPanel Grid.Row="0">
                                                        <Grid Width="35" Height="35" ToolTipService.InitialShowDelay="100">
                                                            <Grid.ToolTip>
                                                                <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding MaterialName}" FontSize="12" Foreground="Black"></TextBlock>
                                                            </Grid.ToolTip>
                                                            <Image Source="{Binding MaterialStarImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                            <Image Source="{Binding MaterialImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                        </Grid>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Grid>
                            <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
                            <TextBlock Grid.Row="2" Text="天赋" Style="{StaticResource MyTextBlock}"></TextBlock>
                            <Border Grid.Row="3" Style="{StaticResource HorizontalDivider}"></Border>
                            <TextBlock Grid.Row="4" Text="命座" Style="{StaticResource MyTextBlock}"></TextBlock>
                            <Border Grid.Row="5" Style="{StaticResource HorizontalDivider}"></Border>
                            <TextBlock Grid.Row="6" Text="数值" Style="{StaticResource MyTextBlock}"></TextBlock>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>