<UserControl x:Class="HexereiKatepnha.Views.Database.Database1CharacterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:HexereiKatepnha.ViewModels.Database"
             xmlns:models="clr-namespace:HexereiKatepnha.Models.ModelsForViews.Database"
             d:DataContext="{d:DesignInstance vm:Database1CharacterViewModel}"
             mc:Ignorable="d"
             Background="#FF1E1E1E"
             d:DesignHeight="1080" d:DesignWidth="1720"
             FontFamily="{StaticResource MyFont}">
    <UserControl.Resources>
        <DataTemplate x:Key="CharacterItemTemplate" DataType="{x:Type models:Database1CharacterModel}">
            <Border CornerRadius="5" BorderBrush="White" BorderThickness="1">
                <Grid Width="441">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <Grid>
                                <Image Source="{Binding BackgroundImagePath, IsAsync=True}" Width="240" Height="240" Stretch="UniformToFill"></Image>
                                <Image Source="{Binding ImagePath, IsAsync=True}" Width="240" Height="240" Stretch="UniformToFill"></Image>
                                <Image Source="{Binding ElementImagePath, IsAsync=True}" Width="60" Height="60" Stretch="UniformToFill" HorizontalAlignment="Left" VerticalAlignment="Top"></Image>
                            </Grid>
                            <Image Source="{Binding StarImagePath, IsAsync=True}" Height="20" HorizontalAlignment="Center"></Image>
                            <Viewbox MaxWidth="150" StretchDirection="DownOnly" Margin="0,0,0,0">
                                <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" TextTrimming="CharacterEllipsis"></TextBlock>
                            </Viewbox>
                            <TextBlock Style="{StaticResource MyTextBlock}" FontSize="10" Foreground="#777777" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0">
                                <Run Text="ID:"></Run>
                                <Run Text="{Binding Vid}"></Run>
                            </TextBlock>
                        </StackPanel>
                        <Border Grid.Column="1" Style="{StaticResource VerticalDivider}"></Border>
                        <Grid Grid.Column="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource MyTextBlock}" Text="{Binding BirthdayString}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0"></TextBlock>
                            <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Image Source="{Binding WeaponTypeImagePath, IsAsync=True}" Height="25" Width="25" Stretch="UniformToFill" Margin="10,0,10,0"></Image>
                                <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding WeaponTypeName}" Margin="0,0,0,0"></TextBlock>
                            </StackPanel>
                            <Border Grid.Row="3" Style="{StaticResource HorizontalDivider}"></Border>
                            <ItemsControl Grid.Row="4" ItemsSource="{Binding NeedMaterialList}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"></WrapPanel>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="2">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"></RowDefinition>
                                            </Grid.RowDefinitions>
                                            <StackPanel Grid.Row="0">
                                                <Grid Width="45" Height="45" ToolTipService.InitialShowDelay="100">
                                                    <Grid.ToolTip>
                                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding MaterialName}"></ToolTip>
                                                    </Grid.ToolTip>
                                                    <Image Source="{Binding MaterialStarImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                    <Image Source="{Binding MaterialImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                                </Grid>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Grid>
                    <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Source="{Binding Talent1ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Talent1Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                        <Image Grid.Column="1" Source="{Binding Talent2ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Talent2Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                        <Image Grid.Column="2" Source="{Binding Talent3ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Talent3Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                    </Grid>
                    <Border Grid.Row="3" Style="{StaticResource HorizontalDivider}"></Border>
                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Source="{Binding Ascension1ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Ascension1Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                        <Image Grid.Column="1" Source="{Binding Ascension2ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Ascension2Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                        <Image Grid.Column="2" Source="{Binding Ascension3ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Ascension3Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                        <Image Grid.Column="3" Source="{Binding Ascension4ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Ascension4Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                        <Image Grid.Column="4" Source="{Binding Ascension5ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Ascension5Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                        <Image Grid.Column="5" Source="{Binding Ascension6ImagePath, IsAsync=True}" Height="50" Width="50" Stretch="UniformToFill" ToolTipService.InitialShowDelay="100">
                            <Image.ToolTip>
                                <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding Ascension6Description}"></ToolTip>
                            </Image.ToolTip>
                        </Image>
                    </Grid>
                    <Border Grid.Row="5" Style="{StaticResource HorizontalDivider}"></Border>
                    <ScrollViewer Grid.Row="6" Style="{StaticResource ModernScrollViewer}">
                        <Grid>
                            <ItemsControl ItemsSource="{Binding SimpleLevelStatTable}" Visibility="{Binding DataContext.IsShowLessNumbers, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch"></StackPanel>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding}" AlternationCount="999">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <UniformGrid Rows="1"></UniformGrid>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border BorderThickness="0.5" BorderBrush="#777777">
                                                        <Viewbox StretchDirection="DownOnly">
                                                            <TextBlock x:Name="CellText" Style="{StaticResource MyTextBlock}" Text="{Binding}" HorizontalAlignment="Center"></TextBlock>
                                                        </Viewbox>
                                                    </Border>
                                                    <DataTemplate.Triggers>
                                                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                                            <Setter TargetName="CellText" Property="Foreground" Value="DarkOrange"></Setter>
                                                        </Trigger>
                                                    </DataTemplate.Triggers>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ItemsControl ItemsSource="{Binding FullLevelStatTable}" Visibility="{Binding DataContext.IsShowMoreNumbers, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch"></StackPanel>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding}" AlternationCount="999">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <UniformGrid Rows="1"></UniformGrid>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border BorderThickness="0.5" BorderBrush="#777777">
                                                        <Viewbox StretchDirection="DownOnly">
                                                            <TextBlock x:Name="CellText" Style="{StaticResource MyTextBlock}" Text="{Binding}" HorizontalAlignment="Center"></TextBlock>
                                                        </Viewbox>
                                                    </Border>
                                                    <DataTemplate.Triggers>
                                                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                                            <Setter TargetName="CellText" Property="Foreground" Value="DarkOrange"></Setter>
                                                        </Trigger>
                                                    </DataTemplate.Triggers>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Background="{DynamicResource GlobalBackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="角色" Style="{StaticResource MyTextBlock}" FontSize="30" Margin="30,20,30,5" />
        <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Vertical">
                    <WrapPanel Orientation="Horizontal" Margin="15,0,0,0">
                        <Button Command="{Binding ClickOnElementFilterCommand}" CommandParameter="1" Margin="2">
                            <Image Source="{Binding Element1ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="火"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementFilter}" Value="1">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnElementFilterCommand}" CommandParameter="2" Margin="2">
                            <Image Source="{Binding Element2ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="水"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementFilter}" Value="2">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnElementFilterCommand}" CommandParameter="3" Margin="2">
                            <Image Source="{Binding Element3ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="风"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementFilter}" Value="3">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnElementFilterCommand}" CommandParameter="4" Margin="2">
                            <Image Source="{Binding Element4ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="雷"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementFilter}" Value="4">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnElementFilterCommand}" CommandParameter="5" Margin="2">
                            <Image Source="{Binding Element5ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="草"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementFilter}" Value="5">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnElementFilterCommand}" CommandParameter="6" Margin="2">
                            <Image Source="{Binding Element6ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="冰"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementFilter}" Value="6">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnElementFilterCommand}" CommandParameter="7" Margin="2">
                            <Image Source="{Binding Element7ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="岩"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementFilter}" Value="7">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </WrapPanel>
                    <WrapPanel Orientation="Horizontal" Margin="15,0,0,0">
                        <Button Command="{Binding ClickOnWeaponFilterCommand}" CommandParameter="1" Margin="2">
                            <Image Source="{Binding Weapon1ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="单手剑"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WeaponFilter}" Value="1">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnWeaponFilterCommand}" CommandParameter="2" Margin="2">
                            <Image Source="{Binding Weapon2ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="双手剑"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WeaponFilter}" Value="2">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnWeaponFilterCommand}" CommandParameter="3" Margin="2">
                            <Image Source="{Binding Weapon3ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="长柄武器"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WeaponFilter}" Value="3">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnWeaponFilterCommand}" CommandParameter="4" Margin="2">
                            <Image Source="{Binding Weapon4ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="法器"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WeaponFilter}" Value="4">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnWeaponFilterCommand}" CommandParameter="5" Margin="2">
                            <Image Source="{Binding Weapon5ImagePath}" Stretch="Uniform" Width="55" Height="55" Margin="10" ToolTipService.InitialShowDelay="100">
                                <Image.ToolTip>
                                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="弓"></ToolTip>
                                </Image.ToolTip>
                            </Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WeaponFilter}" Value="5">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </WrapPanel>
                    <WrapPanel Orientation="Horizontal" Margin="15,0,0,0">
                        <Button Command="{Binding ClickOnStarFilterCommand}" CommandParameter="4" Margin="2">
                            <Image Source="{Binding Star4ImagePath}" Stretch="Uniform" Width="55" Height="14" Margin="10,30"></Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding StarFilter}" Value="4">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding ClickOnStarFilterCommand}" CommandParameter="5" Margin="2">
                            <Image Source="{Binding Star5ImagePath}" Stretch="Uniform" Width="55" Height="14" Margin="10,30"></Image>
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ImageButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding StarFilter}" Value="5">
                                            <Setter Property="Background" Value="{DynamicResource GlobalSelectedColor}"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </WrapPanel>
                </StackPanel>
                <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
                <ListBox Grid.Row="2" ItemsSource="{Binding CharacterView}" SelectedItem="{Binding SelectedCharacter, Mode=TwoWay}" Style="{StaticResource ModernListBox}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="15,0,0,0"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:Database1CharacterModel}">
                            <Border Margin="2" CornerRadius="5" BorderBrush="White" BorderThickness="1" Cursor="Hand">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"></RowDefinition>
                                        <RowDefinition Height="Auto"></RowDefinition>
                                    </Grid.RowDefinitions>
                                    <Grid Grid.Row="0">
                                        <Image Source="{Binding BackgroundImagePath, IsAsync=True}" Width="120" Height="120" Stretch="UniformToFill"></Image>
                                        <Image Source="{Binding ImagePath, IsAsync=True}" Width="120" Height="120" Stretch="UniformToFill"></Image>
                                        <Image Source="{Binding ElementImagePath, IsAsync=True}" Width="30" Height="30" Stretch="UniformToFill" HorizontalAlignment="Left" VerticalAlignment="Top"></Image>
                                    </Grid>
                                    <Viewbox Grid.Row="1" MaxWidth="115" StretchDirection="DownOnly" Margin="0,2">
                                        <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" TextTrimming="CharacterEllipsis"></TextBlock>
                                    </Viewbox>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            <Border Grid.Column="1" Style="{StaticResource VerticalDivider}"></Border>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <CheckBox Grid.Row="0" Content="显示详细数据" IsChecked="{Binding IsShowMoreNumbers}" HorizontalAlignment="Center" Style="{StaticResource ModernCheckBoxStyle}" VerticalAlignment="Center" Margin="0,5"></CheckBox>
                <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
                <ContentControl Grid.Row="2" Content="{Binding SelectedCharacter}" ContentTemplate="{StaticResource CharacterItemTemplate}"></ContentControl>
            </Grid>
        </Grid>
    </Grid>
</UserControl>