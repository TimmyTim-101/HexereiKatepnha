<UserControl x:Class="HexereiKatepnha.Views.Database.Database5DungeonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:models="clr-namespace:HexereiKatepnha.Models.ModelsForViews.Database"
             xmlns:vm="clr-namespace:HexereiKatepnha.ViewModels.Database"
             d:DataContext="{d:DesignInstance vm:Database5DungeonViewModel}"
             mc:Ignorable="d"
             Background="{DynamicResource GlobalBackColor}"
             d:DesignHeight="1080" d:DesignWidth="1720"
             FontFamily="{StaticResource MyFont}">
    <UserControl.Resources>
        <DataTemplate x:Key="DungeonDropTemplate" DataType="{x:Type models:DungeonDropItemModel}">
            <StackPanel Orientation="Vertical" Width="50" Margin="2">
                <Grid ToolTipService.InitialShowDelay="100">
                    <Grid.ToolTip>
                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="{Binding MaterialName}"></ToolTip>
                    </Grid.ToolTip>
                    <Image Source="{Binding MaterialStarImagePath}" Height="50" Width="50" Stretch="UniformToFill"></Image>
                    <Image Source="{Binding MaterialImagePath}" Height="50" Width="50" Stretch="UniformToFill"></Image>
                </Grid>
                <StackPanel Visibility="{Binding IsShow, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center">
                    <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding DropNum}" FontSize="12"></TextBlock>
                </StackPanel>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="DungeonCardTemplate" DataType="{x:Type models:Database5DungeonModel}">
            <Border CornerRadius="5" BorderBrush="White" BorderThickness="1" Margin="2" Width="290">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="140"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" ToolTipService.InitialShowDelay="100">
                            <Grid.ToolTip>
                                <ToolTip Background="Transparent" BorderThickness="0" Height="250" MaxWidth="500">
                                    <Image Source="{Binding ImagePath}" Height="250" Stretch="Uniform"></Image>
                                </ToolTip>
                            </Grid.ToolTip>
                            <Image Source="{Binding ImagePath}" Stretch="UniformToFill" HorizontalAlignment="Center"></Image>
                        </Grid>
                        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Viewbox MaxWidth="185" StretchDirection="DownOnly" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2">
                                <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"></TextBlock>
                            </Viewbox>
                            <StackPanel Orientation="Horizontal" Visibility="{Binding IsCost,Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" Margin="0,15,0,0">
                                <Grid HorizontalAlignment="Center" Visibility="{Binding IsTimeLimit, Converter={StaticResource BooleanToVisibilityConverter}}" Width="120">
                                    <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding TimeString}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                                </Grid>
                                <Border Style="{StaticResource VerticalDivider}" Visibility="{Binding IsTimeLimit, Converter={StaticResource BooleanToVisibilityConverter}}"></Border>
                                <Grid Width="60">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding Cost}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                                        <Image Source="{Binding ResinImagePath}" Width="20" Height="20" Stretch="UniformToFill"></Image>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
                    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Visible" Style="{StaticResource ModernScrollViewer}" PreviewMouseWheel="InnerScrollViewerPreviewMouseWheel">
                        <ui:ItemsRepeater ItemsSource="{Binding DropItemList}" ItemTemplate="{StaticResource DungeonDropTemplate}">
                            <ui:ItemsRepeater.Layout>
                                <ui:UniformGridLayout Orientation="Horizontal"></ui:UniformGridLayout>
                            </ui:ItemsRepeater.Layout>
                        </ui:ItemsRepeater>
                    </ScrollViewer>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Background="{DynamicResource GlobalBackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="30,20,30,5">
            <TextBlock Text="秘境 &amp; 魔物" Style="{StaticResource MyTextBlock}" FontSize="30"></TextBlock>
            <Button Style="{StaticResource ImageButton}" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTipService.InitialShowDelay="100" Margin="10,0,0,0" Command="{Binding OpenHelpWebPageCommand}">
                <Image Source="/Resources/Icons/help_30dp_DDDDDD_FILL0_wght400_GRAD0_opsz24.png" Height="30" Width="30" Stretch="UniformToFill"></Image>
                <Button.ToolTip>
                    <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="帮助"></ToolTip>
                </Button.ToolTip>
            </Button>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
        <ui:ItemsRepeaterScrollHost Grid.Row="2">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewer}">
                <ui:ItemsRepeater ItemsSource="{Binding AllDungeonList}" ItemTemplate="{StaticResource DungeonCardTemplate}">
                    <ui:ItemsRepeater.Layout>
                        <ui:UniformGridLayout Orientation="Horizontal"></ui:UniformGridLayout>
                    </ui:ItemsRepeater.Layout>
                </ui:ItemsRepeater>
            </ScrollViewer>
        </ui:ItemsRepeaterScrollHost>
    </Grid>
</UserControl>