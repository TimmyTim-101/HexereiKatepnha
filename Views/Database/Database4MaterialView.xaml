<UserControl x:Class="HexereiKatepnha.Views.Database.Database4MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
             mc:Ignorable="d"
             Background="#FF1E1E1E"
             d:DesignHeight="1080" d:DesignWidth="1720"
             FontFamily="{StaticResource MyFont}">
    <UserControl.Resources>
        <DataTemplate x:Key="MaterialItemTemplate">
            <Border CornerRadius="5" BorderBrush="White" BorderThickness="1" Margin="2">
                <StackPanel Width="230">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Grid Width="100" Height="100" ToolTipService.InitialShowDelay="100">
                                <Grid.ToolTip>
                                    <ToolTip Background="Transparent" BorderThickness="0">
                                        <Grid Width="250" Height="250">
                                            <Image Source="{Binding BackgroundImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                            <Image Source="{Binding ImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                        </Grid>
                                    </ToolTip>
                                </Grid.ToolTip>
                                <Image Source="{Binding BackgroundImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                <Image Source="{Binding ImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                            </Grid>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding StarImagePath, IsAsync=True}" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,5"></Image>
                            <Viewbox MaxWidth="110" StretchDirection="DownOnly">
                                <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5" TextTrimming="CharacterEllipsis"></TextBlock>
                            </Viewbox>
                            <TextBlock Style="{StaticResource MyTextBlock}" FontSize="10" Foreground="#777777" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,10">
                                <Run Text="ID:"></Run>
                                <Run Text="{Binding Vid}"></Run>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Background="{DynamicResource GlobalBackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="材料" Style="{StaticResource MyTextBlock}" FontSize="30" Margin="30,20,30,5" />
        <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
        <ListBox Grid.Row="2" ItemsSource="{Binding AllMaterials}" ItemTemplate="{StaticResource MaterialItemTemplate}" Style="{StaticResource ModernVirtualizingListBox}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <vwp:VirtualizingWrapPanel Orientation="Horizontal" SpacingMode="None" Margin="15,0,0,0"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                                <Border Style="{StaticResource HorizontalDivider}"></Border>
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>
        </ListBox>
    </Grid>
</UserControl>