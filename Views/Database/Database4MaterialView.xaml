<UserControl x:Class="HexereiKatepnha.Views.Database.Database4MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#FF1E1E1E"
             d:DesignHeight="1080" d:DesignWidth="1720"
             FontFamily="{StaticResource MyFont}">
    <UserControl.Resources>
        <DataTemplate x:Key="MaterialItemTemplate">
            <Border Margin="5,5,0,0" CornerRadius="5" BorderBrush="White" BorderThickness="1">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Grid Width="100" Height="100" ToolTipService.InitialShowDelay="100">
                                <Grid.ToolTip>
                                    <ToolTip Background="Transparent" BorderThickness="0">
                                        <Grid Width="250" Height="250">
                                            <Image Source="{Binding BackgroundImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                            <Image Source="{Binding ImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                        </Grid>
                                    </ToolTip>
                                </Grid.ToolTip>
                                <Image Source="{Binding BackgroundImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                                <Image Source="{Binding ImagePath, IsAsync=True}" Stretch="UniformToFill"></Image>
                            </Grid>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding StarImagePath, IsAsync=True}" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,5"></Image>
                            <Viewbox MaxWidth="110" StretchDirection="DownOnly">
                                <TextBlock Style="{StaticResource MyTextBlock}" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5" TextTrimming="CharacterEllipsis"></TextBlock>
                            </Viewbox>
                            <TextBlock Style="{StaticResource MyTextBlock}" FontSize="16" Foreground="#777777" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,10">
                                <Run Text="ID:"></Run>
                                <Run Text="{Binding Vid}"></Run>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Background="{DynamicResource GlobalBackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="材料" Style="{StaticResource MyTextBlock}" FontSize="30" Margin="30,20,30,5" />
        <Border Grid.Row="1" Style="{StaticResource HorizontalDivider}"></Border>
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewer}">
            <StackPanel>
                <TextBlock Text="基础培养素材" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                <ItemsControl ItemsSource="{Binding List1}" ItemTemplate="{StaticResource MaterialItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="230"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
                <TextBlock Text="角色与武器培养素材" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                <ItemsControl ItemsSource="{Binding List2}" ItemTemplate="{StaticResource MaterialItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="230"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
                <TextBlock Text="角色培养素材" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                <ItemsControl ItemsSource="{Binding List3}" ItemTemplate="{StaticResource MaterialItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="230"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
                <TextBlock Text="角色突破素材" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                <ItemsControl ItemsSource="{Binding List4}" ItemTemplate="{StaticResource MaterialItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="230"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
                <TextBlock Text="角色天赋素材" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                <ItemsControl ItemsSource="{Binding List5}" ItemTemplate="{StaticResource MaterialItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="230"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
                <TextBlock Text="武器突破素材" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                <ItemsControl ItemsSource="{Binding List6}" ItemTemplate="{StaticResource MaterialItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="230"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
                <TextBlock Text="地方特产" Style="{StaticResource MyTextBlock}" Margin="20,0"></TextBlock>
                <ItemsControl ItemsSource="{Binding List7}" ItemTemplate="{StaticResource MaterialItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20,0" ItemWidth="230"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>