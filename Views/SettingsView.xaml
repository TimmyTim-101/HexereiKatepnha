<UserControl x:Class="HexereiKatepnha.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:HexereiKatepnha.ViewModels"
             d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
             mc:Ignorable="d"
             Background="{DynamicResource GlobalBackColor}"
             d:DesignHeight="1080" d:DesignWidth="1720"
             FontFamily="{StaticResource MyFont}">
    <Grid Background="{DynamicResource GlobalBackColor}">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewer}">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal" Margin="30,20,30,5">
                    <TextBlock Text="设置" Style="{StaticResource MyTextBlock}" FontSize="30"></TextBlock>
                    <Button Style="{StaticResource ImageButton}" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTipService.InitialShowDelay="100" Margin="10,0,0,0" Command="{Binding OpenHelpWebPageCommand}">
                        <Image Source="/Resources/Icons/help_30dp_DDDDDD_FILL0_wght400_GRAD0_opsz24.png" Height="30" Width="30" Stretch="UniformToFill"></Image>
                        <Button.ToolTip>
                            <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="帮助"></ToolTip>
                        </Button.ToolTip>
                    </Button>
                </StackPanel>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
                <Border CornerRadius="5" BorderThickness="1" BorderBrush="White" Margin="30,20,30,20">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <TextBlock Text="当前主题：" Style="{StaticResource MyTextBlock}"></TextBlock>
                            <TextBlock Text="{Binding CurrentThemeName}" Foreground="{Binding CurrentThemeColor}" Style="{StaticResource MyTextBlock}"></TextBlock>
                        </StackPanel>
                        <WrapPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="0" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/UI_ItemIcon_305.png" Stretch="Uniform" ToolTipService.InitialShowDelay="100">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="风"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="1" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/UI_ItemIcon_307.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="岩"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="2" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/UI_ItemIcon_304.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="雷"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="3" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/UI_ItemIcon_303.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="草"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="4" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/UI_ItemIcon_302.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="水"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="5" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/UI_ItemIcon_301.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="火"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="6" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/UI_ItemIcon_308.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="月"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="7" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/UI_ItemIcon_306.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="冰"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="8" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/Classic.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="经典配色"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                            <Button Command="{Binding ClickOnThemeCommand}" CommandParameter="9" Style="{StaticResource ImageButton}" Height="100" Margin="30,30,0,30">
                                <Image Source="/Resources/Themes/AllBlack.png" Stretch="Uniform">
                                    <Image.ToolTip>
                                        <ToolTip Style="{StaticResource FlutterStyleToolTip}" Content="纯黑配色"></ToolTip>
                                    </Image.ToolTip>
                                </Image>
                            </Button>
                        </WrapPanel>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
                <Border CornerRadius="5" BorderThickness="1" BorderBrush="White" Margin="30,20,30,20">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="上传GOOD格式json以导入数据" Style="{StaticResource MyTextBlock}" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Top"></TextBlock>
                        <Button Style="{StaticResource FunctionButton}" Content="上传GOOD格式json文件" Command="{Binding ClickOnImportJsonCommand}" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Top"></Button>
                        <TextBlock Style="{StaticResource MyTextBlock}" ToolTipService.InitialShowDelay="100" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Top" TextWrapping="Wrap">
                            <Run Text="当前状态："></Run>
                            <Run Text="{Binding TimeString}" Foreground="LightBlue"></Run>
                            <Run Text="{Binding FileName}" Foreground="DarkOrange"></Run>
                            <Run Text="{Binding UploadJsonStatusSign}"></Run>
                            <TextBlock.ToolTip>
                                <ToolTip Visibility="{Binding IsShowDetail, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource FlutterStyleToolTip}">
                                    <StackPanel Width="250">
                                        <TextBlock Text="{Binding UploadJsonStatusMessage}" TextWrapping="Wrap"></TextBlock>
                                    </StackPanel>
                                </ToolTip>
                            </TextBlock.ToolTip>
                        </TextBlock>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource HorizontalDivider}"></Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>